<Window x:Class="AIMuster.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:i ="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:lc="clr-namespace:AIMuster.Converter"
         xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        mc:Ignorable="d"  WindowState="Maximized"
        Title="{DynamicResource Title_MainWindow}" Height="450" Width="800" >
    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <Menu Grid.Row="0">
            <MenuItem Header="{DynamicResource Help_MainWindow}">
                <MenuItem Header="{DynamicResource Set_MainWindow}" >
                    <i:Interaction.Triggers>
                        <i:EventTrigger EventName="Click">
                            <i:InvokeCommandAction Command="{Binding SetCommand}" />
                        </i:EventTrigger>
                    </i:Interaction.Triggers>
                </MenuItem>
                <MenuItem Header="{DynamicResource Exit_MainWindow}" />
            </MenuItem>
        </Menu>


        <Grid Grid.Row="1">
            <ScrollViewer>
                <ItemsControl ItemsSource="{Binding AiModelConfigs}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="{Binding RowCount}" Columns="{Binding ColumnCount}" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>

                            <Border BorderBrush="Gray" BorderThickness="1" Margin="2">
                                <Grid>
                                    <!-- 默认：显示 WebView2 -->
                                    <wv2:WebView2 x:Name="WebViewer"  Source="{Binding ModelUrl}"  Margin="2" Visibility="{Binding IsEnabled ,Converter={StaticResource BoolToVisibility}}" Loaded="WebViewer_Loaded"/>
                                    <!-- 替代：显示下拉框 -->
                                    <ComboBox x:Name="ModelSelector"   ItemsSource="{Binding AvailableModels}"  SelectedItem="{Binding SelectedModel, Mode=TwoWay}"   Margin="2"  Visibility="{Binding IsEnabled ,Converter={StaticResource BoolToVisibility},ConverterParameter=Invert}" />
                                </Grid>
                               
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"></ColumnDefinition>
                <ColumnDefinition Width="0"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            <TextBox Margin="10" Grid.Column="0"  Text="{Binding CueWord,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding  Key="Enter" Command="{Binding KeyEnterCommand}" />
                </TextBox.InputBindings>
                <!--框架-->
                <!--<i:Interaction.Triggers>
                    <i:EventTrigger EventName="KeyDown">
                        <i:InvokeCommandAction Command="{Binding EnterCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>-->
            </TextBox>
        </Grid>
    </Grid>
</Window>
